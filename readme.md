# SparkPacket File Sharer

- To install run
```
git clone https://github.com/sp-file/sharer.git
cd sp-file
pip3 install websockets watchdog aiofiles
launchctl load ~/Library/LaunchAgents/com.sparkpacket.sync.plist
```
- To run the program
```
python3 sparkpacket.py
```
